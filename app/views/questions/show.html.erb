<p>未解決/解決</p>


<h2>タイトル</h2>
<p><%= @question.title %></p>


<p>投稿ユーザー画像：<%= image_tag @question.user.get_profile_image(100,100) %></p>
<p>ユーザーネーム：<%= @question.user.name %></p>

<p>投稿日：<%= @question.created_at.strftime('%Y/%m/%d') %></p>



<h2>本文</h2>
<p><%= @question.body %></p>


<% if @question.user == current_user %>
<p>編集、下書きに戻す、削除ボタン（自分の時）</p>
<%= link_to "編集", edit_question_path(@question.id) %>
<%= link_to "削除", question_path(@question.id), method: :delete, "data-confirm" => "本当に削除しますか？" %>
<% end %>


<div>
  <p>コメント件数：<%= @question.question_comments.count %></p>
  <% @question.question_comments.each do |question_comment| %>
    <p><%= image_tag question_comment.user.get_profile_image(100,100) %></p>
    <%= question_comment.user.name %>
    <%= question_comment.created_at.strftime('%Y/%m/%d') %><%= post_comment.comment %>
    <% if question_comment.user == current_user %>
      <%= link_to "削除", question_question_comment_path(question_comment.question, question_comment), method: :delete %>
    <% end %>  
  <% end %>
</div>
<div>
  <%= form_with model: [@question, @question_comment] do |f| %>
    <%= f.text_area :comment, rows: '5', placeholder: "コメントをここに" %>
    <%= f.submit "送信する" %>
  <% end %>
</div>

<div>
  <p>回答件数：<%= @question.question_answers.count %></p>
  <% @question.question_answers.each do |question_answer| %>
    <p><%= image_tag question_answer.user.get_profile_image(100,100) %></p>
    <%= question_answer.user.name %>
    <%= question_answer.created_at.strftime('%Y/%m/%d') %><%= post_comment.comment %>
    <% if question_answer.user == current_user %>
      <%= link_to "削除", question_question_answer_path(question_answer.question, question_answer), method: :delete %>
    <% end %>  
  <% end %>
</div>
<div>
  <%= form_with model: [@question, @question_answer] do |f| %>
    <%= f.text_area :answer, rows: '5', placeholder: "回答をここに" %>
    <%= f.submit "送信する" %>
  <% end %>
