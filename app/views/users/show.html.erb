<div id="sidebar" class="col-2"><%= render'layouts/sidebar' %></div>
<div id="main" class="col-10">
    
<div class="my-3 d-flex">
  <%= image_tag @user.get_profile_image(100,100) %>
  <div>
    <h2><%= @user.name %></h2>
    <% if @user.id == current_user.id %>
    <!--% if current_user == @user %>-->
      <%= link_to "プロフィール編集", edit_user_path(@user), class:"btn btn-outline-secondary" %>
    <% else %>
      <% if current_user.following?(user) %>
        <%= link_to "フォロー外す", user_relationships_path(user.id), method: :delete %>
      <% else %>
        <%= link_to "フォローする", user_relationships_path(user.id), method: :post %>
      <% end %>
    <% end %>
  </div>
  
  <div class="d-flex  bd-highlight">
    <p>投稿数</p>
    <% user = @user %>
    <div>
      <p><%= link_to user.followings.count, user_followings_path(user) %></p>
      <p>seguiendo</p>
    </div>
    <div>
      <p><%= link_to user.followers.count, user_followers_path(user) %></p>
      <p>seguidores</p>
    </div>
  </div>


</div>

<span><i class="fa-regular fa-star"></i> <%= @user.spanish_level %></span>


<p><i class="fa-solid fa-graduation-cap"></i> <%= @user.certification %></p>

<p><%= @user.introduction %></p>


<!--<p>スペイン語レベル</p>-->
<!--<p>学歴・資格</p>-->

<!--<もしくは<p>フォローボタン</p>-->

<!--<p>投稿数</p><p>フォロワー数</p><p>フォロー数</p>-->

<ul class="nav nav-tabs" id="myTab" role="tablist">
    <li class="nav-item">
      <a class="nav-link active" id="incio-tab" data-toggle="tab" href="#incio" role="tab" aria-controls="incio" aria-selected="true">incio</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" id="articulos-tab" data-toggle="tab" href="#articulos" role="tab" aria-controls="articulos" aria-selected="false">articulos</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" id="palabras-tab" data-toggle="tab" href="#palabras" role="tab" aria-controls="palabras" aria-selected="false">palabras</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" id="preguntas-tab" data-toggle="tab" href="#preguntas" role="tab" aria-controls="preguntas" aria-selected="false">preguntas</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" id="borrador-tab" data-toggle="tab" href="#borrador" role="tab" aria-controls="borrador" aria-selected="false">borrador</a>
    </li>
  </ul>
